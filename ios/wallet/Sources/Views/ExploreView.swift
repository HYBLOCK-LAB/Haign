import SwiftUI

struct ExploreView: View {
    var body: some View {
        NavigationView {
            Text("Explore View")
                .navigationTitle("main.tab.explore".localized())
        }
    }
}
