import SwiftUI

struct ExchangeView: View {
    var body: some View {
        NavigationView {
            Text("Exchange View")
                .navigationTitle("main.tab.exchange".localized())
        }
    }
}
